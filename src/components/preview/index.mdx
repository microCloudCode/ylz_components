---
name: preview
route: /preview
order: 8
sidebar: true
---

import { Playground } from "docz";

# preview 预览组件

## 安装

## 例子

import "antd/dist/antd.css"; // or 'antd/dist/antd.less'
import Preview from "./index.tsx";
import Data from "./testData.ts";
import { Button } from "antd";

<Playground>
  {() => {
    const [data, setData] = React.useState([Data]); //页面数组
    const tap = () => {
      setData(data.concat([1]));
    };
    console.log(data)
    return (
      <div>
        <Button onClick={tap}>点击</Button>
        <Preview
          hideRender={false}
          Data={data[0]}
          onLoad={()=>console.log("我结束了")}
          coverUrl={"http://xm.gwtj.net:8888/upload/temp/tjbg_fm2.png"}
          footUrl={"http://xm.gwtj.net:8888/upload/temp/tjbg_fm1.png"}
        />
      </div>
    );
  }}
</Playground>
